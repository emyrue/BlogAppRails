<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
  </head>
  <body>
    <section class="single-user">
      <img class="profile-picture" src="<%= @user.photo %>" />
      <div class="user-button user-container">
        <div class="user-name"><%= @user.name %></div>
        <div class="user-post-counter">Number of Posts: <%= @user.posts_counter %></div>
      </div>
    </section>
    <section class="bio-container">
    <div class="bio-heading">Bio:</div>
    <div><%= @user.bio %></div>
    </section>
    <% @posts.each do |post| %>
      <section>
        <div><%= post.title %></div>
        <div><%= post.text %></div>
        <div>Comments: <%= post.comments_counter %>, Likes: <%= post.likes_counter %></div>
      </section>
    <% end %>
    <a href="<%= params[:id] %>/posts">
      <button>See all posts</button>
    </a>
  </body>
</html>